import { PagePlaceholder } from "@/components/PagePlaceholder";

const BorrowerActiveLoansScreen = () => {
    return (
        <PagePlaceholder
            title="Empréstimos Ativos (Tomador)"
            description="Visualize o status dos empréstimos ativos contratados como tomador."
        />
    );
};

export default BorrowerActiveLoansScreen;
