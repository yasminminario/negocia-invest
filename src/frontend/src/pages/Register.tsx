import { PagePlaceholder } from "@/components/PagePlaceholder";

const Register = () => {
    return (
        <PagePlaceholder
            title="Cadastro"
            description="Inicie o fluxo de cadastro para criar sua conta na negocia.ai."
        />
    );
};

export default Register;
