# Este arquivo é o Dockerfile do backend.
# Ele define as instruções necessárias para construir a imagem Docker do backend, permitindo que o backend seja executado como um container isolado e portátil.
# Leia mais sobre a arquitetura cloud na seção XX do projeto (README.md).